# Дизайн-система лендинга ДПО

Источник истины для любых правок UI. Извлечена из макетов Canva
(`Современные подходы к разработке систем ДЗЗ и космической связи(1)/*.png`)
и зафиксирована как сознательное решение, а не как «измерение по скриншоту».

Перед изменением интерфейса прочитай этот файл целиком и переиспользуй токены.
Не вводи одноразовые цвета, размеры, радиусы, тени и отступы вне системы.

## Направление

Тёмный космический лендинг по макетам Canva: чередование чёрных секций,
сине-градиентных блоков и светлых лент; огромные тонкие заголовки;
моноширинные подписи в верхнем регистре; градиентные «пилюли» и глассморфизм.

Важно: градиенты, стекло, пилюли и «блобы» здесь — НЕ generic AI-эстетика,
а утверждённая клиентская система из Canva. Разрешено использовать только
варианты, описанные ниже. Запрещено добавлять новые декоративные приёмы
(неон, анимированные градиенты, parallax, 3D, эмодзи-иконки, KPI-карточки,
бейджи статусов без доменного смысла).

Приоритет UX: читаемость, сканируемость, понятная навигация, доступность.

## Цвета

### Базовые токены (`:root` в `styles.css`)

| Токен | Значение | Роль |
| --- | --- | --- |
| `--bg` | `#030507` | фон страницы |
| `--black` | `#05070b` | фон чёрных секций |
| `--blue-deep` | `#0a2a5e` | тёмно-синий (градиенты) |
| `--blue` | `#1a5fc4` | основной синий |
| `--blue-bright` | `#2f7de1` | яркий акцент |
| `--blue-ice` | `#a9d6f5` | светлый акцент, hover-ссылки |
| `--ice` | `#cfe9fb` | ледяной голубой (ленты) |
| `--text` | `#f4f8fd` | основной текст |
| `--muted` | `#9fb4cc` | вторичный текст |
| `--line` | `rgba(170,205,245,.22)` | границы на тёмном |

### Градиентные рецепты секций (только эти)

- Hero/чёрные секции: `radial-gradient(ellipse at 85% -10%, rgba(30,90,180,.35), transparent 55%)` поверх `--black`.
- Светлая лента (edu, learning): `linear-gradient(175deg,#d3ecfc 0%,#8ec3ee 38%,#3f83d8 72%,#1a5fc4 100%)`.
- Программа: `linear-gradient(160deg,#0d2f66 0%,#1a63c4 50%,#4a96e8 85%,#8ec9f2 100%)`.
- Результаты (outcomes): `linear-gradient(180deg,#3f83d8 0%,#8ec3ee 45%,#d3ecfc 100%)`.
- Контакт: `linear-gradient(180deg,#04101f 0%,#0d2f66 38%,#2f6fd0 72%,#a9d6f5 100%)`.
- Кнопка: `linear-gradient(135deg,#3f8de0 0%,#1a5fc4 55%,#0f3f8f 100%)`; bright — `#7fb9f2→#2f7de1→#1a5fc4`; light — `#eaf5ff→#a9d2f5→#5b9de6` (тёмный текст `#0a2a55`).
- Пилюля-элемент: `linear-gradient(120deg,rgba(70,130,210,.85),rgba(25,60,120,.55) 60%,rgba(120,170,225,.35))`; на светлых лентах — белое стекло с тёмным текстом `#0b2c56`.

### Стекло (glass)

`background: linear-gradient(150deg, rgba(170,205,245,.14), rgba(30,60,110,.10) 55%, rgba(170,205,245,.05)); border: 1px solid rgba(190,220,250,.28); backdrop-filter: blur(14px)`.

## Типографика

Локальные self-hosted шрифты (`assets/fonts/`, woff2, кириллица+латиница,
`font-display:swap`, подключаются через `@import` в начале `styles.css`).
Внешние CDN не используются.

- Sans (заголовки, текст): **Manrope** (variable 200–800) → fallback `"Helvetica Neue", Arial, sans-serif`.
- Mono (подписи, кнопки, кикеры): **JetBrains Mono** (400, 700) → fallback `"PT Mono", "Courier New", monospace`.

Шкала:

| Элемент | Параметры |
| --- | --- |
| `.display` (H2 секций) | 300, `clamp(40px,5.6vw,84px)`, lh 1.06, ls −.03em |
| H1 hero | 600, `clamp(28px,2.6vw,40px)`, lh 1.18 |
| `.mono` | 13px, lh 1.75 |
| `.mono-label` / кикеры | 11px, uppercase, ls .14em, цвет `#8fb4dd` |
| Кнопки | 700 13px mono, uppercase, ls .08em |
| Пилюли-элементы | 13px mono, uppercase, ls .05em, lh 1.55 |
| Подписи фигур | 10px mono, uppercase, ls .1em |
| Длинные тексты (learning-columns) | sans 14.5px, lh 1.7, обычный регистр — uppercase mono только для коротких подписей |

## Отступы и сетка

- Контейнер: `min(1240px, 100% − 96px)`; ≤800px: `100% − 40px`; ≤560px: `100% − 36px`.
- Паддинги секций: 110px desktop → 76px (≤800px) → 64px (≤560px).
- Межблочные gap: 14/16/18/20/24/34/36/40/44/52/60/64/70/80 — не выдумывать промежуточные.
- `scroll-padding-top`: 96px (88px ≤800px) — якоря не прячутся под шапку.

## Формы и радиусы

- Пилюли (кнопки, элементы списков): `border-radius: 999px`.
- Карточки/фигуры: 22–28px; hero-карточка и contact-art: 42–44px; фото: 26–36px.
- Границы предпочтительнее теней; тени только у кнопок и hero-фото.

## Компоненты

- `.btn-pill` — CTA; варианты: `-bright` (primary в hero), `-ghost` (secondary), `-light` (на светлом/контакте). Одна primary-действие на экран.
- `.pill-item` — строка-ссылка/элемент; `.pill-link` со стрелкой `↗` и `.pill-idx` нумерацией.
- `.kicker` — номер раздела с линией; `.kicker-dark` на светлых лентах.
- `.glass` — карточки, hero-карточка, stats-pill, mission-card, contact-art.
- `.gif-toggle` — пауза/запуск GIF (стеклянный круг 46px, иконки на чистом CSS, `aria-pressed`).
- `dialog#link-notice` — уведомление о незаполненной ссылке; закрытие кнопкой, Escape, кликом по подложке.
- `.to-top` — плавающая кнопка «наверх» после 600px скролла (только под `.js`).
- Декор: только `.blob` (hero/edu), `aria-hidden`. Рукописные линии/стрелки не используем.

- Пилюли-элементы: `text-shadow: 0 1px 8px rgba(5,20,45,.5)` для контраста белого текста; на ярко-синей секции программы — затемнённое стекло `rgba(9,30,66,.42)`.

## Состояния (обязательны)

- hover: кнопки поднимаются (`translateY(-3px)`) + brightness; pill-link сдвигается `translateX(10px)`.
- focus-visible: `outline: 3px solid #b3e7fb; outline-offset: 5px` у всех интерактивных.
- `prefers-reduced-motion`: без анимаций и smooth-scroll; reveal отключён; GIF стартуют на паузе.
- Без JS: весь контент виден (reveal только под `.js`), якоря работают, есть `<noscript>`-предупреждение.
- Незаполненные ссылки (`links.js` = `null`) открывают диалог, а не фиктивные страницы.

## Анти-паттерны (запрещено)

- Новые цвета/градиенты/радиусы вне токенов; одноразовые значения «под глаз».
- Внешние шрифты, CDN-библиотеки, трекеры, аналитика.
- Эмодзи как иконки интерфейса; иконки без `aria-label`.
- Выдуманные URL и QR-коды; фиктивная регистрация; «lorem ipsum».
- Скрытие контента средствами только JS; горизонтальный скролл на любой ширине.
- SaaS-клише вне системы: KPI-карточки, бейджи «new/active», маркетинговые иллюстрации-стоки.

## Медиа

- Фото/GIF — только локальные из `assets/`; источники — `originals/` (не публикуются).
- GIF зациклены (`-loop 0`), 10 fps, 560px; у каждого постер `*-poster.webp` и кнопка паузы.
- Логотипы: `logo-kadry.webp` (кадрыдлякосмоса.рф), `logo-fakt.webp` (fakt.mipt.ru) — белые на прозрачном; не перекрашивать.

## Проверка перед завершением работы

1. `node --test tests.mjs` — контракт ссылок/диалога/меню.
2. `node visual-qa.mjs` — рендер в Chromium на 1440/1024/768/390/320: переполнение,
   загрузка изображений, якоря, диалог, меню, пауза GIF, reveal, «наверх»,
   ошибки JS; скриншоты в `qa/`.
3. Сверить скриншоты с этим файлом и макетами Canva; перечислить конкретные
   дефекты, исправить, прогнать заново. Не отчитываться «выглядит хорошо» без скриншотов.
